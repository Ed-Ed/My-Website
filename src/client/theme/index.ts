import { createTheme } from '@edwardandrewb/elements';

const theme = createTheme({
  palette: {
    background: {
      default: '#040404',
    },
  },
  typography: {
    fontFamily: "'Lato', sans-serif",
  },
});

export { theme };
